# 安全边界

> **所属系列**：[MaeDoc 核心架构设计](./index.md)
> **最后更新**：2026-02-22

---

## 安全措施

| 层级 | 安全措施 |
|------|---------|
| **文件访问** | 禁止访问 `.env`、`credentials` 等敏感文件（AGENTS.md 定义） |
| **外部请求** | 仅 `/escalate` 可生成外发请求，且需经过敏感信息扫描 |
| **写入确认** | 任何覆盖操作必须经过用户显式确认 |
| **审计日志** | `.docforge/` 记录所有远程交互（可选启用） |

---

## 设计原则

### 本地优先

所有敏感操作在本地完成，核心资产和敏感文档不离开本地工作流。

### 显式授权

远程模型仅在用户主动触发 `/escalate` 时介入，且外发内容必须经过敏感信息扫描。

### 可追溯

所有远程交互都会记录到 `.docforge/` 目录（可选启用审计日志）。

---

## 相关文档

- [系统全景](./system-overview.md) — 了解 OpenCode 运行时与外部网络的边界
- [AGENTS.md](../../AGENTS.md) — 完整的安全红线定义
